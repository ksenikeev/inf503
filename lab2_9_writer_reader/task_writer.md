1. Используя PrintWriter и ByteArrayOutputStream сформировать
массив байт, содержащий текст из нескольких строчек.
Получить массив и записать его в файл в кодировке cp1251
2. Прочитать текст из файла с помощью FileReader, если слово начинается
с буквы А и заканчивается буквой Н, то перевести его в верхний регистр,
результат записать с помощью FileWriter в новый файл
   (использовать метод mark(30) и reset())
3. Строку преобразовать в массив байт, на базе этого массива создать ByteArrayInputStream,
на базе этого объекта сформировать InputStreamReader, в цикле считать и распечатать все символы
4. Скопировать файл посимвольно, использовать FileReader и FileWriter
5. Скопировать файл построчно, использовать BufferedReader и BufferedWriter
6. Скопировать файл побайтно, используя FileInputStream и FileOutputStream
7. Скопировать файл, используя BufferedInputStream и BufferedOutputStream
8. Скопировать файл, используя FileInputStream и FileOutputStream и метод transferTo

В задачах 4 - 8 замерить время работы программы (new Date().getTime())